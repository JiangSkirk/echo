"""CheckpointManager has been removed.

This module previously managed git shadow-repo snapshots for agent state
checkpointing. It was fully functional but had zero runtime callers.

If checkpoint functionality is needed in the future, restore from git history.
"""
